---
interface Props {
  code: string;
  language?: string;
}

const { code, language = 'text' } = Astro.props;
---

<pre class="bg-slate-100 p-4 rounded mt-2 overflow-x-auto">
  <code class={`language-${language}`}>{code}</code>
</pre>